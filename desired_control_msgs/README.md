# desired_control_msgs

The `DesiredControl` message package. **Supplied by the model side and committed here verbatim** — field names, types and
order exactly as the publisher defines them, or the topic silently fails to connect. Semantics are in the top-level README.
Awaiting the `.msg` (promised 2026-09-08).
